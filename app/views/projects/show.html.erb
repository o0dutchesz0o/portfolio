<p style="color: green"><%= notice %></p>

<%= render @project %>

<div>
  <%= link_to "Edit this Project", edit_project_path(@project) if logged_in?(:site_admin) %> |
  <%= link_to "Back to Projects", projects_path %>

  <%= button_to "Delete this Project", @project, method: :delete, data: { turbo_confirm: "Are you sure you want to delete this Project?"} if logged_in?(:site_admin) %>
</div>
